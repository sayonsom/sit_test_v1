# Backend API (alignbackendapis)
#
# Option A (recommended for local dev): call Cloud Run directly
REACT_APP_API_URL=https://alignbackendapis-708196257066.asia-southeast1.run.app/api/v1
#
# Option B: if you run `node proxy-server.js`, you can point to the local proxy
# REACT_APP_API_URL=http://localhost:4000/api/v1

# LTI Backend API (for local testing with ngrok)
# Replace with your ngrok domain or local URL
REACT_APP_LTI_API_URL=http://localhost:8000

# For UAT/prod behind nginx, use same-origin proxying:
# REACT_APP_API_URL=/api/v1
# REACT_APP_LTI_API_URL=  (omit/blank to default to window.location.origin)

# Note: Auth0 variables are no longer needed after migration
