# Production/UAT Environment Variables
#
# If you deploy with Docker, prefer runtime configuration via `docker-entrypoint.sh`
# (it generates `/env-config.js` at container start).
#
# Backend API (nginx proxies `/api/v1/*` to the upstream backend)
REACT_APP_API_URL=/api/v1

# LTI Backend API
# Note: frontend code appends `/lti/...` to this base URL.
# Leave blank to default to `window.location.origin` at runtime.
REACT_APP_LTI_API_URL=
